services:
  - type: web
    name: arghosite-job-finder
    runtime: python
    rootDir: WebApp_ArghoSite
    buildCommand: pip install -r requirements.txt
    startCommand: python -m gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
